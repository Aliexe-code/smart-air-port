# Environment variables for Smart Airport Application
# Generated by Ansible on {{ ansible_date_time.iso8601 }}

# Application Configuration
NODE_ENV={{ node_env | default('production') }}
PORT={{ app_port | default(3001) }}
APP_URL=http://{{ ansible_host }}:{{ app_port | default(3001) }}

# JWT Configuration
JWT_SECRET={{ jwt_secret | default('AASFLQPLD1020DASDLA0SD0L0DW02') }}
JWT_REFRESH_SECRET={{ jwt_refresh_secret | default('LASDACASFQW112521@RT2=3') }}

# Database Configuration
MONGO_URI={{ mongo_uri | default('mongodb://localhost:27017/smartairport') }}

# Redis Configuration
REDIS_HOST={{ redis_host | default('localhost') }}
REDIS_PORT={{ redis_port | default(6379) }}
REDIS_PASSWORD={{ redis_password | default('') }}

# Mail Configuration
MAIL_HOST={{ mail_host | default('smtp.gmail.com') }}
MAIL_PORT={{ mail_port | default(587) }}
MAIL_USER={{ mail_user | default('') }}
MAIL_PASSWORD={{ mail_password | default('') }}
MAIL_FROM={{ mail_from | default('"Smart Airport" <noreply@smartairport.com>') }}

# Stripe Configuration
STRIPE_SECRET_KEY={{ stripe_secret_key | default('') }}
STRIPE_PUBLIC_KEY={{ stripe_public_key | default('') }}
STRIPE_WEBHOOK_SECRET={{ stripe_webhook_secret | default('') }}

# Amadeus API Configuration
AMADEUS_API_KEY={{ amadeus_api_key | default('') }}
AMADEUS_API_SECRET={{ amadeus_api_secret | default('') }}

# PayMob Configuration
PAYMOB_API_KEY={{ paymob_api_key | default('') }}
PAYMOB_MERCHANT_ID={{ paymob_merchant_id | default('') }}
PAYMOB_HMAC_SECRET={{ paymob_hmac_secret | default('') }}
PAYMOB_CARD_INTEGRATION_ID={{ paymob_card_integration_id | default('') }}
PAYMOB_IFRAME_ID={{ paymob_iframe_id | default('') }}

# Frontend Configuration
FRONTEND_URL={{ frontend_url | default('https://sky-shifters.vercel.app') }}

# Booking Configuration
BOOKING_TIMEOUT_MINUTES={{ booking_timeout_minutes | default(5) }}
