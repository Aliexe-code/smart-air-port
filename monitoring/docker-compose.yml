services:
  # Prometheus - Metrics collection and alerting
  prometheus:
    image: prom/prometheus:latest
    container_name: smart-airport-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - ./prometheus/alert_rules.yml:/etc/prometheus/alert_rules.yml
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--storage.tsdb.retention.time=200h'
      - '--web.enable-lifecycle'
      - '--web.enable-admin-api'
    restart: unless-stopped
    networks:
      - monitoring


  # Node Exporter - System metrics
  node-exporter:
    image: prom/node-exporter:latest
    container_name: smart-airport-node-exporter
    ports:
      - "9100:9100"
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    command:
      - '--path.procfs=/host/proc'
      - '--path.rootfs=/rootfs'
      - '--path.sysfs=/host/sys'
      - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)'
    restart: unless-stopped
    networks:
      - monitoring

  # AlertManager - Alert handling
  alertmanager:
    image: prom/alertmanager:latest
    container_name: smart-airport-alertmanager
    ports:
      - "9093:9093"
    volumes:
      - ./alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml
      - alertmanager_data:/alertmanager
    command:
      - '--config.file=/etc/alertmanager/alertmanager.yml'
      - '--storage.path=/alertmanager'
      - '--web.external-url=http://localhost:9093'
    restart: unless-stopped
    networks:
      - monitoring

  # Smart Airport Application (your app)
  smart-airport:
    image: uriel25x/grad-project:latest
    container_name: smart-airport-app
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - JWT_SECRET=AASFLQPLD1020DASDLA0SD0L0DW02
      - JWT_REFRESH_SECRET=LASDACASFQW112521@RT2=3
      - MONGO_URI=mongodb+srv://maged:3333@cluster0.ueawaen.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
      - REDIS_HOST=localhost
      - REDIS_PORT=6379
      - REDIS_PASSWORD=1asfalsp123ed
      - FRONTEND_URL=https://sky-shifters.vercel.app
      - APP_URL=https://sky-shifters.duckdns.org
      - MAIL_HOST=smtp.gmail.com
      - MAIL_PORT=587
      - MAIL_USER=nestjst@gmail.com
      - MAIL_PASSWORD=llne nrbs buvv uaft
      - MAIL_FROM=NestJS Test <nestjst@gmail.com>
    restart: unless-stopped
    networks:
      - monitoring
      - app-network

  # MongoDB Exporter (optional)
  mongodb-exporter:
    image: bitnami/mongodb-exporter:latest
    container_name: smart-airport-mongodb-exporter
    ports:
      - "9216:9216"
    environment:
      - MONGODB_URI=mongodb+srv://maged:3333@cluster0.ueawaen.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
    restart: unless-stopped
    networks:
      - monitoring

  # Redis Exporter (optional)
  redis-exporter:
    image: oliver006/redis_exporter:latest
    container_name: smart-airport-redis-exporter
    ports:
      - "9121:9121"
    environment:
      - REDIS_ADDR=redis://localhost:6379
      - REDIS_PASSWORD=1asfalsp123ed
    restart: unless-stopped
    networks:
      - monitoring

volumes:
  prometheus_data:
  alertmanager_data:

networks:
  monitoring:
    driver: bridge
  app-network:
    driver: bridge
